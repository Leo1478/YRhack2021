//cuz idk how chrome extension works, but might as well do something
function detectHere() {
    var element = document.getElementsByClassName("oIy2qc");
    var length = element.length;
    var latest = element[length - 1].toLowerCase();
    return (latest == "here");
}
//what am i doing
